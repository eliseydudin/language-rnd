# lib
Currently this directory just contains the plans I have for the language, see `grammar.cofy` for the syntax. In the future this directory will contain the source code for the language's standard library.
